package trishul.test.trishul_p;
import trishul_p.*;

public class PrintlnReplaceEngine
extends Engine
{
  	public Order query (Action a)
  	{
    	aswitch(a)
    	{
      	case <* trishul.test.trishul_p.*.getString(..)>: return new ReplaceOrder("Replaced string");
    	}
	    return null;
  	}
}
